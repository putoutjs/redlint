{
    "printer": "putout",
    "rules": {
        "filesystem/remove-files": ["on", {
            "names": ["coverage"]
        }],
        "remove-unused-variables": "off",
        "remove-console": "off",
        "nodejs/declare": "off"
    },
    "ignore": [
        "!**/coverage"
    ]
}
